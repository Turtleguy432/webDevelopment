#include <stdio.h>

int arrayReturn();
int sortArray();

int main(int argc, char* argv[])
{
	int unsortedArray = arrayReturn();
	for (int i = 0; i < sizeof(unsortedArray); ++i)
	{
		printf("%d\n", unsortedArray[i] );
	
	}

	return 0;
}

int arrayReturn()
{
	randomNum=rand()
	int array[] = "";
	sizeof(array) = getchar(); 


}